server:
  port: ${PORT:5000}

base-url: /api

spring:

  jpa:
    hibernate:
      ddl-auto: update

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml

springdoc:
  swagger-ui:
    enabled: true
